name: Build Codex Claude Workstation Image

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Published image tag; leave empty for the default release tag"
        default: ""
        type: string
      platforms:
        description: "Comma-separated target platforms"
        default: linux/amd64,linux/arm64
        type: string
      push_latest:
        description: "Also publish latest"
        default: true
        type: boolean
  schedule:
    - cron: "17 3 * * 0"
  pull_request:
    paths:
      - 'codex-claude-workstation-builder/**'
      - '.github/workflows/build-codex-claude-workstation.yml'
      - '.github/workflows/release-workstations.yml'
      - 'scripts/smoke-codex-workstation.py'
      - 'scripts/trivy-image-gate.sh'
      - 'scripts/evaluate-trivy-policy.py'
      - 'scripts/test-evaluate-trivy-policy.py'
      - 'scripts/publish-tested-image.py'
      - 'scripts/test-publish-tested-image.py'

permissions:
  contents: read
  packages: write

concurrency:
  group: build-codex-workstation-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  release:
    uses: ./.github/workflows/release-workstations.yml
    with:
      variant: codex
      image_tag: ${{ inputs.image_tag || '' }}
      platforms: ${{ inputs.platforms || 'linux/amd64,linux/arm64' }}
      push_latest: ${{ github.event_name == 'schedule' || inputs.push_latest }}
      publish: ${{ github.event_name != 'pull_request' && github.ref == 'refs/heads/main' }}
